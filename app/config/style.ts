import { StyleSheet } from 'react-native'
import colors from './colors'

const styles = StyleSheet.create({
  frenchProductDesc: { fontSize: 16, color: colors.$DFS_red, lineHeight:17}
})

export const {frenchProductDesc} = styles
